# 实验进度记录

- 记录时间：2026-04-30 05:23:16 +08
- 记录范围：`routing/src` 的 TRIM-Rubric 离线 pipeline
- 目标：将此前 OmniMath 泛化版 TRIM-Rubric 改为与 TRIM-Agg 相同的数据集对口训练/测试
- 当前状态：正在生成 train episodes，尚未进入 rubric discovery / PPO / eval

## 一、实验设定

### 数据集

#### Math 链路

- 训练集：`trim/TRIM/math_eval/data/math/train_1k.jsonl`
- 训练 episodes：`src/data/episodes/math_train_1k_episodes.jsonl`
- 目标规模：`1100`
- 测试集：`trim/TRIM/math_eval/data/math500/test_100.jsonl`
- 测试 episodes：`src/data/episodes/math500_test_100_episodes.jsonl`
- 目标规模：`100`

#### AIME 链路

- 训练集：`trim/TRIM/math_eval/data/aime/train.jsonl`
- 训练 episodes：`src/data/episodes/aime_train_episodes.jsonl`
- 目标规模：`451`
- 测试集：`trim/TRIM/math_eval/data/aime/test.jsonl`
- 测试 episodes：`src/data/episodes/aime_test_episodes.jsonl`
- 目标规模：`482`

### 模型配置

- draft / SRM：`/home/deepseek_VG/deepseek_VG/routing/routing/models/qwen3-1.7b`
- target / LRM：`/home/deepseek_VG/deepseek_VG/routing/routing/models/qwen3-14b`
- PRM：`/home/deepseek_VG/deepseek_VG/routing/routing/models/qwen2.5-math-prm-7b`
- generation cap：`max_new_tokens=3072`
- 原因：当前 vLLM 服务 `max_model_len=4096`，不能继续使用旧的 `16384` completion 上限

## 二、服务层状态

当前采用三卡折中方案：

| 角色 | GPU | 端口 | 状态 | 说明 |
|---|---:|---:|---|---|
| Math target | 0 | 30000 | OK | Qwen3-14B |
| shared draft | 1 | 30001 | OK | Qwen3-1.7B，Math/AIME 共用 |
| shared PRM | 1 | 30002 | OK | Qwen2.5-Math-PRM-7B，Math/AIME 共用 |
| AIME target | 7 | 31000 | OK | Qwen3-14B |
| AIME draft | 7 | 31001 | DOWN | 未使用 |
| AIME PRM | 7 | 31002 | DOWN | 未使用 |

说明：

- 最初尝试在 GPU7 上同时放 AIME target + draft + PRM。
- AIME target 成功启动，但 draft 在 GPU7 上因 vLLM KV cache 空间不足失败。
- 因此当前 AIME 只独占 target，draft/PRM 复用 GPU1 的 `30001/30002`。
- 当前有效链路：
  - Math：`30001 draft -> 30000 target -> 30002 PRM`
  - AIME：`30001 draft -> 31000 target -> 30002 PRM`

## 三、运行中的任务

### screen 会话

```text
trim_rubric_math_chain
trim_rubric_aime_chain
```

### 当前进程

#### Math

```bash
python -u -m data.generate_episodes \
  --dataset math_train_1k \
  --output_dir data/episodes \
  --srm_server_url http://localhost:30001/v1 \
  --lrm_server_url http://localhost:30000/v1 \
  --srm_model_name /home/deepseek_VG/deepseek_VG/routing/routing/models/qwen3-1.7b \
  --lrm_model_name /home/deepseek_VG/deepseek_VG/routing/routing/models/qwen3-14b \
  --prm_server_url http://localhost:30002 \
  --prm_model_name /home/deepseek_VG/deepseek_VG/routing/routing/models/qwen2.5-math-prm-7b \
  --max_new_tokens 3072 \
  --max_workers 4
```

#### AIME

```bash
python -u -m data.generate_episodes \
  --dataset aime_train \
  --output_dir data/episodes \
  --srm_server_url http://localhost:30001/v1 \
  --lrm_server_url http://localhost:31000/v1 \
  --srm_model_name /home/deepseek_VG/deepseek_VG/routing/routing/models/qwen3-1.7b \
  --lrm_model_name /home/deepseek_VG/deepseek_VG/routing/routing/models/qwen3-14b \
  --prm_server_url http://localhost:30002 \
  --prm_model_name /home/deepseek_VG/deepseek_VG/routing/routing/models/qwen2.5-math-prm-7b \
  --max_new_tokens 3072 \
  --max_workers 4
```

## 四、当前进度

截至本次记录：

| 数据 | 当前行数 | 目标行数 | 状态 |
|---|---:|---:|---|
| `math_train_1k_episodes.jsonl` | 46 | 1100 | 正在生成 |
| `math500_test_100_episodes.jsonl` | 0 | 100 | 未开始 |
| `aime_train_episodes.jsonl` | 32 | 451 | 正在生成 |
| `aime_test_episodes.jsonl` | 0 | 482 | 未开始 |

### 当前日志估算

#### Math

- 当前进度：`46/1100`
- 日志 ETA：约 `17.2h`
- 当前 running acc：
  - `SRM=24/40 (60.0%)`
  - `LRM=25/40 (62.5%)`

#### AIME

- 当前进度：`32/451`
- 日志 ETA：约 `10.0h`
- 当前 running acc：
  - `SRM=10/30 (33.3%)`
  - `LRM=12/30 (40.0%)`

## 五、后续自动链路

### Math 链路

1. 生成 `math_train_1k_episodes.jsonl`
2. 生成 `math500_test_100_episodes.jsonl`
3. 基于 `math_train_1k_episodes.jsonl` 生成：
   - `src/data/rubrics/math_train_1k/rubric_weights.json`
   - `src/data/rubrics/math_train_1k/rubric_consistency.json`
   - `src/data/rubrics/math_train_1k/episode_rubric_scores.jsonl`
4. 训练 TRIM-Rubric PPO：
   - cost 点：`8e-4, 6e-4, 5e-4, 4e-4, 3e-4`
   - checkpoint：`src/checkpoints/trim_rubric_math1k_sweep/{cost}/best.pt`
5. 在 `math500_test_100_episodes.jsonl` 上评估：
   - 输出：`src/results/trim_rubric_math1k_sweep/{cost}/`

### AIME 链路

1. 生成 `aime_train_episodes.jsonl`
2. 生成 `aime_test_episodes.jsonl`
3. 基于 `aime_train_episodes.jsonl` 生成：
   - `src/data/rubrics/aime_train/rubric_weights.json`
   - `src/data/rubrics/aime_train/rubric_consistency.json`
   - `src/data/rubrics/aime_train/episode_rubric_scores.jsonl`
4. 训练 TRIM-Rubric PPO：
   - cost 点：`3e-4, 2.5e-4, 2e-4, 1.5e-4, 1e-4, 8e-5`
   - checkpoint：`src/checkpoints/trim_rubric_aime_official_sweep/{cost}/best.pt`
5. 在 `aime_test_episodes.jsonl` 上评估：
   - 输出：`src/results/trim_rubric_aime_official_sweep/{cost}/`

## 六、日志与监控命令

### 查看进度

```bash
cd /home/deepseek_VG/deepseek_VG/routing/routing/src

wc -l data/episodes/math_train_1k_episodes.jsonl
wc -l data/episodes/aime_train_episodes.jsonl

tail -f logs/trim_rubric_math_prepare.log
tail -f logs/trim_rubric_aime_prepare.log
```

### 查看 screen

```bash
screen -ls
screen -r trim_rubric_math_chain
screen -r trim_rubric_aime_chain
```

### 查看服务健康

```bash
for port in 30000 30001 30002 31000 31001 31002; do
  printf '%s ' "$port"
  curl -sf --max-time 2 http://localhost:${port}/health >/dev/null && echo OK || echo DOWN
done
```

## 七、已处理的问题

1. **原始启动脚本路径不适配当前机器**
   - 旧脚本中 `/home/chencheng` 和 `/mnt/hdd2/chengcheng` 不存在。
   - 当前改用本机路径 `/home/deepseek_VG/deepseek_VG/routing/routing/models/*`。

2. **GPU7 无法同时承载 AIME target + draft + PRM**
   - draft 启动时报 vLLM KV cache 空间不足。
   - 当前改为 AIME target 独占 GPU7，draft/PRM 复用 GPU1。

3. **`MAX_NEW_TOKENS=16384` 超过 vLLM context**
   - 服务 `max_model_len=4096`。
   - 当前生成使用 `--max_new_tokens 3072`。

4. **后台 `nohup` 不稳定**
   - 曾出现导入后无 traceback 退出。
   - 当前改用 detached `screen`，与此前 TRIM-Agg 实验运行方式一致。

## 八、2026-04-30 17:16 更新

### 当前状态

- 主准备链路已恢复运行：
  - screen：`trim_rubric_prepare_resume`
  - 日志：`src/logs/trim_rubric_prepare_resume.log`
- Math train episodes 已从 `308/1100` 断点续跑，目前已追加到 `310/1100`。
- AIME train episodes 仍在等待 Math 链路完成后继续，从 `331/451` 断点续跑。
- `math500_test_100_episodes.jsonl` 和 `aime_test_episodes.jsonl` 仍未生成，会在 train episodes 补齐后按脚本顺序生成。

### 本次修复

1. `src/vllm_client.py`
   - 识别 vLLM context-length 报错。
   - 根据报错中的 `max_model_len` 和 prompt token 数，自动降低 `max_tokens` 后重试一次。
   - 避免单条长题因为 `prompt_tokens + 3072 > max_model_len` 直接中断整条链路。

2. `src/scripts/prepare_trim_rubric_data.sh`
   - 不再仅凭 episodes 文件存在就跳过。
   - 按目标行数判断完整度：
     - `math_train_1k=1100`
     - `math500_test_100=100`
     - `aime_train=451`
     - `aime_test=482`
   - 如果文件存在但行数不足，会继续调用 `generate_episodes.py` 的 resume 逻辑。
   - 当前 AIME 也复用 `30001 draft / 30000 target / 30002 PRM`，避免依赖暂时不可用的 GPU7 `31000` 服务。

3. `src/scripts/start_trim_rubric_vllm_8192.sh`
   - 新增当前机器可复用的 vLLM 启动脚本。
   - 当前健康服务：
     - `30000`：Qwen3-14B target，`max_model_len=8192`
     - `30001`：Qwen3-1.7B draft，`max_model_len=8192`
     - `30002`：Qwen2.5-Math-PRM-7B，`max_model_len=4096`
   - PRM 模型 config 最大长度为 4096，未强行覆盖。

### 注意事项

- GPU7 上已有其它训练进程占用约 22GB 显存，`31000` 的 AIME target 无法重新启动：
  - `8192` context：KV cache 显存不足。
  - `4096` context + `gpu_memory_utilization=0.45`：仍无可用 KV cache。
- 为避免影响其它训练，当前没有杀 GPU7 上的非本实验进程。
- 继续监控：

```bash
screen -ls
screen -r trim_rubric_prepare_resume

tail -f /home/deepseek_VG/deepseek_VG/routing/routing/src/logs/trim_rubric_prepare_resume.log

wc -l /home/deepseek_VG/deepseek_VG/routing/routing/src/data/episodes/math_train_1k_episodes.jsonl
wc -l /home/deepseek_VG/deepseek_VG/routing/routing/src/data/episodes/aime_train_episodes.jsonl
```
