# 实验进度记录

- 记录时间：2026-04-28 03:46:54 +08
- 记录范围：TRIM 官方代码 `routing/trim/TRIM`
- 当前模型配置：
  - target：`/mnt/hdd2/chengcheng/qwen3-14b`
  - draft：`/mnt/hdd2/chengcheng/qwen3-1.7b`
  - PRM：`/mnt/hdd2/chengcheng/qwen2.5-math-prm-7b`

## 一、Math Sweep 进度

当前运行的是 `qwen3_math1k_sweep`，训练/评测配置为：

- 训练集：`math/train_1k`
- 测试集：`math500/test_100`
- train/val 切分：`1000 train + 100 val`
- 训练配置：`10 epochs`，`batch_size=64`，`val_every=50`，`eval_every=50`，`seed=10`
- cost 点：`8e-4, 6e-4, 5e-4, 4e-4, 3e-4`

### 已完成结果

#### 1. cost_per_token = 8e-4

- checkpoint：
  - `/home/chencheng/routing/trim/TRIM/rlpolicy_checkpoints_qwen3_math1k_sweep/8e-4/policy_best.pt`
- eval 结果：
  - `/home/chencheng/routing/trim/TRIM/rlpolicy_results_qwen3_math1k_sweep/8e-4/eval_metrics.jsonl`
- 指标：
  - `accuracy = 0.03`
  - `num_correct = 3 / 100`
  - `avg_total_tokens_per_question = 68.48`
  - `avg_target_tokens_per_question = 0.0`
  - `avg_target_calls_per_question = 0.0`

#### 2. cost_per_token = 6e-4

- checkpoint：
  - `/home/chencheng/routing/trim/TRIM/rlpolicy_checkpoints_qwen3_math1k_sweep/5.999999999999999e-4/policy_best.pt`
- eval 结果：
  - `/home/chencheng/routing/trim/TRIM/rlpolicy_results_qwen3_math1k_sweep/5.999999999999999e-4/eval_metrics.jsonl`
- 指标：
  - `accuracy = 0.63`
  - `num_correct = 63 / 100`
  - `avg_total_tokens_per_question = 473.52`
  - `avg_target_tokens_per_question = 0.41`
  - `avg_target_calls_per_question = 0.01`

### 正在运行

#### 3. cost_per_token = 5e-4

- supervisor：
  - `/home/chencheng/routing/trim/TRIM/scripts/run_qwen3_math1k_remaining_sweep.sh`
- 运行进程：
  - `PID 3154162`：`TRIM_Agg.py --mode train ... --cost_per_token 5e-4`
- 当前日志：
  - `/home/chencheng/routing/trim/TRIM/logs/trim_agg_math1k_5e-4.log`
- 最新进度：
  - `2026-04-28 03:46:05 [INFO] Batch 22`
  - 期间出现过一次 PRM 请求重试：
    - `2026-04-28 03:32:03 [WARNING] PRM request failed ... retrying in 2.0s`
  - 训练仍在继续，后续会自动接 `5e-4 eval -> 4e-4 train/eval -> 3e-4 train/eval`

### 后续待自动执行

- `5e-4 eval`
- `4e-4 train + eval`
- `3e-4 train + eval`

## 二、AIME 2020-2024 Sweep 准备情况

目标：把 `routing/src` 中关于 AIME 的数据思路接到官方 `TRIM` 代码上进行训练和测试。

### 数据来源与处理

- 参考的 `src` 逻辑：
  - `routing/src/config.py` 中的 `LAMBDA_AIME = [3e-4, 2.5e-4, 2e-4, 1.5e-4, 1e-4, 8e-5]`
  - `routing/src/common/datasets.py` 中的 `load_aime(year_from=2020, year_to=2024)`
- 实际源文件：
  - `/home/chencheng/routing/data/aime_1983_2024.jsonl`
- 已新增官方 TRIM 兼容的数据准备脚本：
  - `/home/chencheng/routing/trim/TRIM/scripts/prepare_aime2020_2024_dataset.py`

### 已生成的官方 TRIM 数据集

- 生成目录：
  - `/home/chencheng/routing/trim/TRIM/math_eval/data/aime2020_2024/`
- train 文件：
  - `/home/chencheng/routing/trim/TRIM/math_eval/data/aime2020_2024/train.jsonl`
- test 文件：
  - `/home/chencheng/routing/trim/TRIM/math_eval/data/aime2020_2024/test.jsonl`
- 数据规模：
  - `train = 59`（2020-2024 的 AIME Part I）
  - `test = 74`（2020-2024 的 AIME Part II）

说明：

- 这份 `AIME 2020-2024` 子集规模很小，不存在 `1000 train + 100 val`。
- 后续官方 TRIM 训练会在 `59` 条 train 上再按 `val_fraction` 切分验证集。

### 已做的官方代码兼容修改

- 已在官方 parser 中加入 `aime2020_2024` 支持：
  - `/home/chencheng/routing/trim/TRIM/math_eval/parser.py`

### 已配置的 AIME cost sweep

- 采用 `src` 中的 AIME lambda 配置：
  - `3e-4`
  - `2.5e-4`
  - `2e-4`
  - `1.5e-4`
  - `1e-4`
  - `8e-5`

### 已创建的运行脚本

- AIME sweep 主脚本：
  - `/home/chencheng/routing/trim/TRIM/scripts/run_qwen3_aime2020_2024_sweep.sh`
- 等待 Math sweep 完成后自动启动的脚本：
  - `/home/chencheng/routing/trim/TRIM/scripts/run_qwen3_aime2020_2024_after_math.sh`

### 当前状态

- 已后台挂起等待进程：
  - `PID 3189737`
- 等待日志：
  - `/home/chencheng/routing/trim/TRIM/logs/trim_agg_aime2020_2024_after_math.log`
- 当前等待日志状态：
  - `2026-04-28 03:44:49 [info] waiting for math sweep to finish before starting AIME 2020-2024`
  - `2026-04-28 03:46:49 [wait] math sweep incomplete; sleeping 60s`

## 三、当前后台链概览

### Math 链

- 进程：
  - `PID 3154119`
  - `PID 3154160`
  - `PID 3154162`
- 作用：
  - 按顺序完成 `5e-4 -> 4e-4 -> 3e-4` 的 train/eval

### AIME 链

- 进程：
  - `PID 3189737`
- 作用：
  - 等 Math 5 点 curve 全部完成后，自动启动 `AIME 2020-2024` 的 6 点 train/eval sweep

## 四、备注

- `routing/src` 与 `routing/trim/TRIM` 不是同一套实验框架。
- 当前实际执行的是官方 `TRIM` 代码，AIME 的 cost 设置和数据选择借鉴了 `src` 中的逻辑。
- 若后续需要出 trade-off 图，可以在 `AIME` sweep 完成后统一从 `eval_metrics.jsonl` 汇总绘图。
